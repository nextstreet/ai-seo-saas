# n8n exports

Store reviewed, sanitized workflow JSON exports here. Never commit credentials,
webhook secrets, execution history, or tenant data.

Naming convention: `<domain>--<action>--v<number>.json`.
