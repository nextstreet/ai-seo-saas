# Content Engine

Builds page-type-aware Briefs, assembles prompts with facts, validates drafts,
and supports refreshes. It answers how an approved page should be created. It
does not prioritize SEO opportunities or publish content automatically.

V1 includes page-type-aware Brief generation and a validated candidate
lifecycle. Publication can only follow `draft -> review -> published`, and
every transition requires an explicit reason.
